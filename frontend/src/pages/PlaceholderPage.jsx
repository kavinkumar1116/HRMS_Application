const PlaceholderPage = ({ title }) => {
  return (
    <section className="health-section">
      <h2>{title}</h2>
      <p>This is a placeholder screen. You can add detailed UI and logic later.</p>
    </section>
  );
};

export default PlaceholderPage;


